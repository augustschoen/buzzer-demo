# Laufzeit-Daten: enthält Konten & Guthaben — gehört NICHT ins öffentliche Repo
server/data.json

# Mail-Zugangsdaten (API-Key) — niemals committen
server/mail-config.json

# Datenbank-Zugang (Neon) — niemals committen
server/db-config.json

# Abhängigkeiten
server/node_modules/

# Generiert (Artifact-Variante von index.html, wird bei Bedarf neu erzeugt)
buzzer-app.html

# Lokale Editor-/Tool-Einstellungen
.claude/
